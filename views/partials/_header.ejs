<header>
    <nav class="navbar navbar-expand-md navbar-dark bg-primary">
        <a class="navbar-brand" href="/urls">TinyApp</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup"  aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
          <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
              <div class="navbar-nav mr-auto mt-2 mt-lg-0">
                <a class="nav-item nav-link" href="/urls">My URLs</a>
                <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
              </div>
              
              <div> 
                <% if (!username) { %>
                <form method="POST" action="/login" class="form-inline my-2 my-lg-0">
                  <input type="text" class="form-control mr-sm-2" name="username" placeholder="username">
                  <button type="submit" class="btn btn-outline-light my-2 my-sm-0">Sign In</button>
                </form>
                <%} else { %>
                  <div class="d-flex">
                      <p class="d-inline-flex pr-5 text-white h3">Hello <%= username %></p>
                      <form method="POST" action="/logout">
                      <button type="submit" class="btn btn-outline-light d-inline-flex">Log Out</button>
                    </form>
                    <!-- <ul class="list-inline">
                      <li class="list-inline-item text-white"><%= username %></li>
                      <li class="list-inline-item"><button type="button" class="btn btn-outline-light col-6">Log Out</button></li>
                    </ul> -->
                </div>
                <%}%>
              </div>
            </div>
        </div>
      </nav>
  </header>

 